{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate --noinput && gunicorn nothing_game.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/api/leaderboard/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
